var Sent1 = "This is the beginning of the string"
var Sent2 = " and this is the end of the string"

window.alert(Sent1); //prints first variable
document.write(Sent1 + Sent2); //Concatenates both variables together