function myFunction() { //begins function
    var sentence = "This is part one,"; //defines variable
    sentence += " and this is part two"; //adds onto variable
    document.getElementById("Combine").innerHTML=sentence; //creates HTML element that calls function
}